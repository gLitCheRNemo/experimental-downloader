name: Twitch VODs Scraper

on:
  workflow_dispatch:
    inputs:
      channel_url:
        description: 'Twitch channel URL'
        required: true
        type: string
      link_range:
        description: 'Which VOD links to extract (e.g., 1, 1-5, 3,7,10-15)'
        required: false
        default: '1-10'
        type: string
      scroll_count:
        description: 'Number of times to scroll down (0-20)'
        required: false
        default: '3'
        type: string

jobs:
  scrape-vods:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          mkdir -p scraper
          cd scraper
          npm init -y
          npm install puppeteer-extra puppeteer-extra-plugin-stealth puppeteer

      - name: Create scraper script
        run: |
          cat > scraper/index.js << 'EOF'
          const puppeteer = require('puppeteer-extra');
          const StealthPlugin = require('puppeteer-extra-plugin-stealth');
          const fs = require('fs');
          
          puppeteer.use(StealthPlugin());

          function expandRange(rangeStr, maxValue) {
            var result = [];
            var parts = rangeStr.split(',');
            for (var p = 0; p < parts.length; p++) {
              var part = parts[p].trim();
              if (part.indexOf('-') !== -1) {
                var rangeParts = part.split('-');
                var start = parseInt(rangeParts[0], 10);
                var end = parseInt(rangeParts[1], 10);
                var min = Math.min(start, end);
                var max = Math.max(start, end);
                for (var i = min; i <= max && i <= maxValue; i++) {
                  if (result.indexOf(i) === -1) result.push(i);
                }
              } else if (part !== '') {
                var num = parseInt(part, 10);
                if (!isNaN(num) && num <= maxValue && result.indexOf(num) === -1) {
                  result.push(num);
                }
              }
            }
            result.sort(function(a, b) { return a - b; });
            return result;
          }

          (async function() {
            var browser = null;
            try {
              console.log('Launching Chrome...');
              browser = await puppeteer.launch({
                executablePath: '/usr/bin/google-chrome-stable',
                args: [
                  '--no-sandbox',
                  '--disable-setuid-sandbox',
                  '--disable-dev-shm-usage',
                  '--disable-blink-features=AutomationControlled'
                ],
                headless: 'new'
              });

              var page = await browser.newPage();
              await page.setViewport({ width: 1920, height: 1080 });
              await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36');

              var channelUrl = process.env.CHANNEL_URL;
              channelUrl = channelUrl.replace(/\/$/, '');
              var videosUrl = channelUrl;
              if (channelUrl.indexOf('/videos') === -1) {
                videosUrl = channelUrl + '/videos';
              }

              console.log('Navigating to: ' + videosUrl);
              await page.goto(videosUrl, { waitUntil: 'networkidle2', timeout: 60000 });
              await new Promise(function(r) { setTimeout(r, 5000); });

              var scrollCount = parseInt(process.env.SCROLL_COUNT, 10) || 3;
              for (var s = 0; s < scrollCount; s++) {
                await page.evaluate(function() { window.scrollBy(0, window.innerHeight); });
                await new Promise(function(r) { setTimeout(r, 2000); });
              }
              await new Promise(function(r) { setTimeout(r, 3000); });

              var vods = [];
              var seenUrls = {};

              var cards = await page.$$('a[href*="/videos/"], a[href*="/v/"]');

              for (var c = 0; c < cards.length; c++) {
                try {
                  var href = await page.evaluate(function(el) { return el.getAttribute('href'); }, cards[c]);
                  if (!href) continue;
                  if (href.indexOf('/') === 0) href = 'https://www.twitch.tv' + href;
                  var cleanUrl = href.split('?')[0];
                  if (cleanUrl.indexOf('/videos/') === -1 && cleanUrl.indexOf('/v/') === -1) continue;
                  if (seenUrls[cleanUrl]) continue;
                  seenUrls[cleanUrl] = true;

                  var title = await page.evaluate(function(el) {
                    var card = el.closest('[data-test-selector="video-card"], .tw-card, article');
                    if (card) {
                      var t = card.querySelector('[data-test-selector="video-card-title"], .tw-title, h3, h4');
                      if (t) return t.textContent.trim();
                    }
                    return 'Untitled';
                  }, cards[c]);

                  vods.push({ url: cleanUrl, title: title || 'Untitled' });
                } catch(e) {}
              }

              if (vods.length === 0) {
                var pageContent = await page.content();
                var matches = pageContent.match(/https:\/\/www\.twitch\.tv\/videos\/\d+/g);
                if (matches) {
                  var uniqueMatches = {};
                  for (var m = 0; m < matches.length; m++) {
                    if (!uniqueMatches[matches[m]]) {
                      uniqueMatches[matches[m]] = true;
                      vods.push({ url: matches[m], title: 'VOD' });
                    }
                  }
                }
              }

              if (vods.length === 0) {
                console.log('NO_VODS_FOUND');
                process.exit(1);
              }

              var range = process.env.LINK_RANGE || '1-10';
              var indices = expandRange(range, vods.length);
              var selected = [];

              for (var i = 0; i < indices.length; i++) {
                var idx = indices[i];
                if (idx >= 1 && idx <= vods.length) selected.push(vods[idx - 1]);
              }

              var output = '';
              var links = '';
              for (var i = 0; i < selected.length; i++) {
                output = output + (i+1) + '. ' + selected[i].title + '\n   ' + selected[i].url + '\n\n';
                links = links + selected[i].url + '\n';
              }

              // Write to files that will be preserved
              fs.writeFileSync('/tmp/vod_links.txt', links);
              fs.writeFileSync('/tmp/vod_full_list.txt', output);
              fs.writeFileSync('/tmp/link_count.txt', String(selected.length));

              var channelParts = channelUrl.replace(/\/$/, '').split('/');
              var channelName = channelParts[channelParts.length - 1];
              fs.writeFileSync('/tmp/channel_name.txt', channelName);

              console.log('SUCCESS');
              await browser.close();
            } catch(err) {
              console.log('ERROR: ' + err.message);
              process.exit(1);
            } finally {
              if (browser) await browser.close();
            }
          })();
          EOF

      - name: Run scraper
        id: run_scraper
        run: |
          cd scraper
          export CHANNEL_URL="${{ inputs.channel_url }}"
          export LINK_RANGE="${{ inputs.link_range }}"
          export SCROLL_COUNT="${{ inputs.scroll_count }}"
          OUTPUT=$(node index.js 2>&1)
          echo "$OUTPUT"
          if echo "$OUTPUT" | grep -q "NO_VODS_FOUND"; then
            echo "status=no_vods" >> $GITHUB_OUTPUT
          elif echo "$OUTPUT" | grep -q "SUCCESS"; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=error" >> $GITHUB_OUTPUT
          fi

      - name: Send success to Bale
        if: steps.run_scraper.outputs.status == 'success'
        run: |
          VOD_LINKS=$(cat /tmp/vod_links.txt 2>/dev/null || echo "")
          VOD_FULL_LIST=$(cat /tmp/vod_full_list.txt 2>/dev/null || echo "")
          LINK_COUNT=$(cat /tmp/link_count.txt 2>/dev/null || echo "0")
          CHANNEL_NAME=$(cat /tmp/channel_name.txt 2>/dev/null || echo "Unknown")
          TOKEN="${{ secrets.BALE_TOKEN }}"
          CHAT_ID="${{ secrets.BALE_CHAT_ID }}"
          
          curl -s -X POST "https://tapi.bale.ai/bot${TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "text=✅ Twitch VODs Extracted!%0A%0A📺 Channel: ${CHANNEL_NAME}%0A📋 VODs found: ${LINK_COUNT}%0A%0A${VOD_FULL_LIST}%0A💡 Use these with your Video Downloader."

      - name: Send no VODs message to Bale
        if: steps.run_scraper.outputs.status == 'no_vods'
        run: |
          TOKEN="${{ secrets.BALE_TOKEN }}"
          CHAT_ID="${{ secrets.BALE_CHAT_ID }}"
          CHANNEL="${{ inputs.channel_url }}"
          curl -s -X POST "https://tapi.bale.ai/bot${TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "text=⚠️ No VODs found for ${CHANNEL}%0A%0APossible reasons:%0A- Channel has no past broadcasts%0A- Channel requires login to view VODs%0A- Twitch is blocking the request"

      - name: Send error message to Bale
        if: steps.run_scraper.outputs.status == 'error'
        run: |
          TOKEN="${{ secrets.BALE_TOKEN }}"
          CHAT_ID="${{ secrets.BALE_CHAT_ID }}"
          CHANNEL="${{ inputs.channel_url }}"
          curl -s -X POST "https://tapi.bale.ai/bot${TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "text=❌ Failed to scrape VODs for ${CHANNEL}%0A%0AThe scraper encountered an error. This may be due to:%0A- Twitch anti-bot protection%0A- Network issues%0A- Channel page not loading properly"

      - name: Cleanup
        run: rm -rf scraper /tmp/vod_* 2>/dev/null || true